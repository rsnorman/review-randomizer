<%= simple_form_for([@team, @team_membership]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :handle, autofocus: true %>
    <%= f.association :user %>
  </div>

  <div>
    <%= f.button :submit %>
  </div>
<% end %>
