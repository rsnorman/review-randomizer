<%= simple_form_for(@team) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :repos %>
  </div>

  <div class="form-inputs">
    <%= f.button :submit %>
  </div>
<% end %>
